<odoo>
    <record id="dispatch_route_tree" model="ir.ui.view">
        <field name="name">dispatch.route.list</field>
        <field name="model">dispatch.route</field>
        <field name="arch" type="xml">
            <list>
                <field name="code_route" readonly="True"/>
                <field name="sale_order_ids" widget="many2many_tags"/>
                <field name="outside_route_sales_order_ids" widget="many2many_tags"/>
                <field name="route_date"/>
                <field name="vehicle_id"/>
                <field name="capacity_list_view"/>
<!--                <field name="vehicle_capacity"/>-->
            </list>
        </field>
    </record>

</odoo>