<odoo>

        <!-- Vista tipo lista para montos de descuento ISSS -->
        <record id="view_hr_retencion_isss_tree" model="ir.ui.view">
            <field name="name">hr.retencion.isss.tree</field>
            <field name="model">hr.retencion.isss</field>
            <field name="arch" type="xml">
                <list>
                    <field name="tipo"/>
                    <field name="porcentaje"/>
                    <field name="techo"/>
                </list>
            </field>
        </record>

        <!-- Vista tipo formulario para montos de descuento ISSS -->
        <record id="view_hr_retencion_isss_form" model="ir.ui.view">
            <field name="name">hr.retencion.isss.form</field>
            <field name="model">hr.retencion.isss</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="tipo"/>
                            <field name="porcentaje"/>
                            <field name="techo"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
</odoo>
